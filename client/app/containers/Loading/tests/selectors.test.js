// import { fromJS } from 'immutable';
// import { makeSelectLoadingDomain } from '../selectors';

// const selector = makeSelectLoadingDomain();

describe('makeSelectLoadingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
