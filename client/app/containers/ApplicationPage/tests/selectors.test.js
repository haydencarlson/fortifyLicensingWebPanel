// import { fromJS } from 'immutable';
// import { makeSelectApplicationPageDomain } from '../selectors';

// const selector = makeSelectApplicationPageDomain();

describe('makeSelectApplicationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
